# find the deepest node in a binary search tree
# take in a list of numbers
# Give you a class called Node (value, Lch, Rch)
#  you know you've found the deepest node if the node has no children
# test cases: INPUT(9) OUTPUT([18,10])

####################################################################
# Need to research this

class Node():
    """Building the tree"""
    def _init_(value, left, right):
        self.val = val
        self.left = left
        self.right = right
        self.level = level

    def New_node(tree):
        """ Find the deepest node in a tree"""
        def _init_():
            self.node = node
            self.level = level

def LabelLevel(node, current):



######################################################################


def sorted_list(n_sorted_lists):
    ans_list =[]

    for lst in n_sorted_lists:
        ans_list.extend(lst)
    ans_list = sorted(ans_list)
    return ans_list
























